//! Out-of-process integration tests for the split dora services.
//!
//! This crate has no library API; it exists to host the `tests/` harness that
//! spawns the `dora-migrate`, `dora-v4`, `dora-v6`, and `dora-api` binaries.
