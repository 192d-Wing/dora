apiVersion: apps/v1
kind: Deployment
metadata:
  name: usg-dora-v6-server
  namespace: {{ .Release.Namespace }}
  labels: &labels
    {{- include "dora.labels" . | nindent 4 }}
    app.kubernetes.io/name: usg-dora-v6-server
    app.kubernetes.io/component: dhcpv6
    dora.io/workload: usg-dora-v6_server
spec:
  replicas: {{ .Values.replicas.v6 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: usg-dora-v6-server
  template:
    metadata:
      labels: *labels
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: usg-dora-v6-server
                topologyKey: kubernetes.io/hostname
      initContainers:
        {{- include "dora.initMigrate" . | nindent 8 }}
      containers:
        - name: dora
          image: {{ include "dora.image" (dict "root" . "image" .Values.image.v6) }}
          args:
            - "-c"
            - "/etc/dora/config.yaml"
            - "--v6-addr"
            - "[::]:547"
          env:
            {{- include "dora.envCommon" . | nindent 12 }}
          ports:
            - name: dhcpv6
              containerPort: 547
              protocol: UDP
          volumeMounts:
            - name: config
              mountPath: /etc/dora
              readOnly: true
          resources:
            {{- toYaml .Values.resources.v6 | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: dora-config
---
apiVersion: v1
kind: Service
metadata:
  name: usg-dora-v6-server
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dora.labels" . | nindent 4 }}
    app.kubernetes.io/name: usg-dora-v6-server
    dora.io/lb-pool: anycast
  annotations:
    io.cilium/lb-ipam-ips: {{ .Values.vips.ipv6 | quote }}
spec:
  type: LoadBalancer
  ipFamilyPolicy: SingleStack
  ipFamilies: [IPv6]
  externalTrafficPolicy: Local
  selector:
    app.kubernetes.io/name: usg-dora-v6-server
  ports:
    - name: dhcpv6
      port: 547
      targetPort: dhcpv6
      protocol: UDP
