apiVersion: v1
kind: ConfigMap
metadata:
  name: dora-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dora.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "dora.doraConfig" . | nindent 4 }}
